{# templates/reclamation/new.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Add Reclamation{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ block('form_stylesheets') }}
{% endblock %}

{% block body %}
    {% include 'ClientHome/header.html.twig' with {'page_title': 'Add Reclamation'} %}

    <div class="container-fluid py-4">
        <div class="row justify-content-center">
            <div class="col-lg-8 col-md-10">
                <div class="card">
                    <div class="card-header">
                        Add Reclamation
                    </div>
                    <div class="card-body">
                        {{ include('reclamation/_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
