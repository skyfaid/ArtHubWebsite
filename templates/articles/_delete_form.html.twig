<form method="post" action="{{ path('app_articles_delete', {'articleId': article.articleId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.articleId) }}">
    <button class="btn">Delete</button>
</form>
