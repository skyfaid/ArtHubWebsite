<form method="post" action="{{ path('app_solution_delete', {'solutionid': solution.solutionid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ solution.solutionid) }}">
    <button class="btn">Delete</button>
</form>
